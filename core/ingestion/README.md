# Core Ingestion

Stores normalized canonical transaction records from inbound adapters.

This module should validate records, persist valid transactions, record ingestion errors, and enforce deduplication using source identity.
