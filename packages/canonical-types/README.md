# Canonical Types

Generated or maintained types for the Ledgerise canonical transaction schema.

Types should be derived from `schemas/transaction.schema.json` once the toolchain is selected.

For the current scaffold, these are maintained TypeScript interfaces aligned to `schemas/transaction.schema.json`. Keep this package and the schema in sync when canonical fields change.
